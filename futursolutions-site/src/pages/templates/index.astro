---
import BaseLayout from '../../layouts/BaseLayout.astro';
import FeaturedFoundation from '../../components/FeaturedFoundation.astro';
import DemoCard from '../../components/DemoCard.astro';
import ServiceFAQSection from '../../components/ServiceFAQSection.astro';
import { templateSystems, templateBrowseFilterDefs } from '../../data/templates';

const featuredSlug = 'premium-wellness-clinic';
const gridDemos = templateSystems.filter((t) => t.slug !== featuredSlug);
const newDemoSlugs = new Set(['premium-wellness-clinic', 'hormone-trt']);

// "Anatomy of a Foundation" layers for the mega-section graphic. Each layer maps
// 1:1 to an includeCategories entry, so the diagram doubles as a visual index.
const foundationLayers = [
	'Strategy',
	'Design',
	'Pages',
	'Lead path',
	'SEO & tracking',
	'Editable content',
	'Launch',
];

// Subtle Growth-System flow shown in the add-on chapter.
const growthFlow = ['Website', 'Form', 'CRM', 'Follow-up', 'Reporting'];

const pageTitle = 'Website Foundations | FutureSolutions';
const pageDescription =
	'A premium website foundation, customized around your business. Start from a proven FutureSolutions framework and shape it around your brand, services, and lead path — the smart middle path between cheap templates and fully custom.';
const canonicalUrl = 'https://futuresolutions.io/templates/';

const heroChips = [
	'From $3,500',
	'Customized framework',
	'Faster than fully custom',
	'Growth-system ready',
];

// "What is a Website Foundation" — a not-this / this contrast keeps the offer
// from reading like a cheap template.
const notThis = [
	'A generic theme reskin',
	'Placeholder page structure',
	'A pretty design with no lead path',
	'Important content locked behind a developer',
];
const insteadThis = [
	'A proven structure customized to your business',
	'Service pages built around visitor intent',
	'A clear call-to-action and booking path',
	'Editable content where it matters',
	'Forms, tracking, and follow-up readiness',
];

const bestForCards = [
	'Outdated sites that need a professional rebuild',
	'Wellness clinics and med spas',
	'Solo practices and consultants',
	'Hormone / TRT and high-trust health brands',
	'Local and appointment-based service businesses',
	'Teams that need a clearer path from visitor to inquiry',
];

const includeCategories = [
	{
		title: 'Strategy & structure',
		items: ['Offer clarity', 'Page map', 'Visitor journey', 'Service-page planning'],
	},
	{
		title: 'Design customization',
		items: ['Brand adaptation', 'Section styling', 'Imagery direction', 'Mobile-first layout'],
	},
	{
		title: 'Core pages',
		items: ['Homepage', 'Service pages', 'About', 'Contact / booking', 'FAQs & trust sections', 'Optional resources structure'],
	},
	{
		title: 'Lead path',
		items: ['CTA hierarchy', 'Inquiry / booking path', 'Form placement', 'Follow-up readiness'],
	},
	{
		title: 'SEO & tracking foundation',
		items: ['SEO-aware structure', 'Metadata basics', 'Local visibility', 'GA4 / GSC / GTM readiness'],
	},
	{
		title: 'Editable content',
		items: ['Sanity CMS where useful', 'Key content stays manageable after launch'],
	},
	{
		title: 'Launch support',
		items: ['Responsive QA', 'Form testing', 'Deployment', 'Post-launch walkthrough'],
	},
];

const processSteps = [
	{ step: '01', title: 'Free Snapshot & discovery', text: 'We review your current site, lead path, and goals — and confirm a Foundation is the right fit.' },
	{ step: '02', title: 'Foundation direction', text: 'We choose the strongest starting framework for your business, audience, and service model.' },
	{ step: '03', title: 'Structure & content', text: 'We map the pages, service structure, and conversion path, then shape the copy around how visitors decide.' },
	{ step: '04', title: 'Design customization', text: 'We adapt the look, sections, imagery, and messaging to your brand so it feels genuinely custom.' },
	{ step: '05', title: 'Build & connect', text: 'We build the site, connect forms and tracking, and prepare the lead and booking path.' },
	{ step: '06', title: 'Launch & improve', text: 'We test, launch, and recommend next improvements or ongoing management support.' },
];

const comparison = [
	{
		lane: 'foundation',
		name: 'Website Foundation',
		tag: 'Core offer',
		points: [
			'Faster path to launch',
			'Starts from a proven framework',
			'Customized around your brand and services',
			'Best for common high-trust service-business needs',
			'Lower starting investment',
			'Ideal when you need a professional site without a full custom process',
		],
	},
	{
		lane: 'custom',
		name: 'Signature Custom Website',
		tag: 'Premium path',
		points: [
			'Deeper strategy and design',
			'Custom page architecture',
			'Custom content model',
			'Best for complex organizations or unique positioning',
			'Higher investment',
			'Ideal when a framework approach would be too limiting',
		],
	},
];

const growthAddItems = [
	'CRM pipeline',
	'Booking / calendar routing',
	'Form notifications',
	'Email / SMS follow-up',
	'Review request flow',
	'Reporting and lead visibility',
];

const pricingTiers = [
	{
		name: 'Foundation Starter',
		price: 'From $3,500',
		text: 'A focused site foundation for a clean, professional launch.',
		featured: false,
	},
	{
		name: 'Foundation Plus',
		price: 'Typically $5,000–$6,500',
		text: 'More pages, deeper service structure, stronger content support, and added launch preparation.',
		featured: true,
	},
	{
		name: 'Foundation + Growth System',
		price: 'Quoted after your Snapshot',
		text: 'Your Website Foundation plus CRM, booking, follow-up, and reporting setup.',
		featured: false,
	},
];

const foundationFaqs = [
	{
		question: 'Is this just a template?',
		answer:
			'No. A Website Foundation starts from a proven framework, then we customize the structure, content, and design around your business. The result is a real, conversion-ready site — not a prebuilt theme you have to force your business into.',
	},
	{
		question: 'What can be customized?',
		answer:
			'Your brand, colors, imagery, services, page structure, copy, and lead path. The strategic structure stays intact while everything that makes the site yours is tailored.',
	},
	{
		question: 'Can I edit the site myself?',
		answer:
			'Yes, where it matters. Key content areas are set up in an editable CMS (such as Sanity) so you can update text and content after launch without breaking the design.',
	},
	{
		question: 'What pages are included?',
		answer:
			'Typically a homepage, a services overview and individual service pages, an about page, and a contact or booking page, plus trust and FAQ sections. Final page count depends on your business and is confirmed during the Snapshot.',
	},
	{
		question: 'Do you write the copy?',
		answer:
			'Yes. Conversion-focused copy and service-page structure are part of the build, organized around how your visitors actually decide. You stay involved so everything is accurate to your business.',
	},
	{
		question: 'Can you connect it to GoHighLevel or another CRM?',
		answer:
			'Yes. Forms, booking, and lead routing can connect to GoHighLevel or a similar CRM as a Growth System — at launch or later.',
	},
	{
		question: 'What if I need something fully custom?',
		answer:
			'Then a Signature Custom Website is the better path. If a Foundation would be too limiting, we will say so in your Snapshot and recommend the custom route.',
	},
	{
		question: 'How long does it take?',
		answer:
			'Because the structure is already proven, Foundations typically launch in weeks rather than months. We map a realistic timeline during the Snapshot before any work begins.',
	},
	{
		question: 'What happens after launch?',
		answer:
			'You can manage editable content yourself, or add an ongoing management plan for updates, content, SEO structure, reporting, and continued optimization.',
	},
	{
		question: 'Do I need a Free Snapshot first?',
		answer:
			'It is the best starting point. The free Website & Lead Flow Snapshot reviews your situation and recommends whether a Foundation, a custom build, or something simpler fits — with no pressure.',
	},
];
---

<BaseLayout
	title={pageTitle}
	description={pageDescription}
	canonicalUrl={canonicalUrl}
	ogTitle={pageTitle}
	ogDescription={pageDescription}
	ogUrl={canonicalUrl}
>
	<!-- SECTION 1 — HERO -->
	<section class="section templates-hero" aria-labelledby="templates-heading">
		<div class="site-container templates-hero__grid">
			<div class="templates-hero__copy">
				<p class="eyebrow">Website Foundations</p>
				<h1 id="templates-heading">A premium website foundation, customized around your business.</h1>
				<p class="lead">
					Start with a proven FutureSolutions framework, then shape it around your brand, services, audience, and lead path — so you can launch faster without starting from a blank page or settling for a generic template.
				</p>
				<ul class="templates-hero__chips" aria-label="Website Foundation highlights">
					{heroChips.map((chip, i) => (
						<li class:list={[{ 'is-accent': i === 0 }]}>{chip}</li>
					))}
				</ul>
				<div class="button-row">
					<a class="button button-primary" href="/audit">Get My Free Snapshot →</a>
					<a class="button button-secondary" href="#foundation-examples">Explore Foundation Examples</a>
				</div>
				<p class="templates-hero__micro">Free Loom review · Clear recommendation · No pressure</p>
			</div>

			<div class="templates-hero__media">
				<div class="foundation-visual" aria-hidden="true">
					<span class="fv-deco fv-deco--back"></span>
					<span class="fv-deco fv-deco--mid"></span>
					<div class="fv-browser">
						<div class="fv-browser__bar">
							<span class="fv-browser__dot"></span>
							<span class="fv-browser__dot"></span>
							<span class="fv-browser__dot"></span>
							<span class="fv-browser__url">yourpractice.com</span>
						</div>
						<div class="fv-screen">
							<div class="fv-screen__hero">
								<span class="fv-pill">Trusted local care</span>
								<span class="fv-bar fv-bar--title"></span>
								<span class="fv-bar fv-bar--title fv-bar--title-2"></span>
								<span class="fv-bar fv-bar--text"></span>
								<div class="fv-screen__cta">
									<span class="fv-btn">Book a visit</span>
									<span class="fv-tag">Conversion-ready</span>
								</div>
							</div>
							<div class="fv-services">
								<div class="fv-card">
									<span class="fv-card__icon"></span>
									<span class="fv-bar fv-bar--sm"></span>
									<span class="fv-bar fv-bar--xs"></span>
								</div>
								<div class="fv-card">
									<span class="fv-card__icon"></span>
									<span class="fv-bar fv-bar--sm"></span>
									<span class="fv-bar fv-bar--xs"></span>
								</div>
								<div class="fv-card">
									<span class="fv-card__icon"></span>
									<span class="fv-bar fv-bar--sm"></span>
									<span class="fv-bar fv-bar--xs"></span>
								</div>
							</div>
							<div class="fv-trust">
								<span class="fv-stars">★★★★★</span>
								<span class="fv-bar fv-bar--trust"></span>
							</div>
						</div>
					</div>
					<span class="fv-callout fv-callout--lead">Lead path</span>
					<span class="fv-callout fv-callout--edit">Editable content</span>
					<span class="fv-callout fv-callout--growth">Growth ready</span>
				</div>
			</div>
		</div>
	</section>

	<!-- SECTION 2 — WHAT A WEBSITE FOUNDATION IS -->
	<section class="section templates-define" aria-labelledby="templates-define-heading">
		<div class="site-container templates-define__inner">
			<div class="templates-define__copy">
				<p class="eyebrow">The middle path</p>
				<h2 id="templates-define-heading">What is a Website Foundation?</h2>
				<p class="lead">
					A Website Foundation is a premium, customizable website built from a proven FutureSolutions framework — then tailored around your brand, services, service pages, content needs, and lead path.
				</p>
				<p class="templates-define__support">
					It is not a theme reskin or generic template swap. The structure is planned around what high-trust service businesses actually need: clear services, trust signals, mobile usability, lead capture, and editable content.
				</p>
			</div>
			<div class="templates-define__compare">
				<p class="templates-define__compare-label">Built to avoid the usual website shortcuts.</p>
				<div class="templates-define__cards" role="group" aria-label="What a Website Foundation is not, versus what it is">
					<div class="templates-define__col templates-define__col--not">
						<p class="templates-define__col-label">Not this</p>
						<ul>
							{notThis.map((item) => (
								<li>
									<span class="templates-define__icon templates-define__icon--x" aria-hidden="true">
										<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><line x1="4" y1="4" x2="12" y2="12" /><line x1="12" y1="4" x2="4" y2="12" /></svg>
									</span>
									{item}
								</li>
							))}
						</ul>
					</div>
					<div class="templates-define__col templates-define__col--yes">
						<p class="templates-define__col-label">
							Instead
							<span class="templates-define__col-badge">Recommended</span>
						</p>
						<ul>
							{insteadThis.map((item) => (
								<li>
									<span class="templates-define__icon templates-define__icon--check" aria-hidden="true">
										<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
									</span>
									{item}
								</li>
							))}
						</ul>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- SECTION 3 — BEST FOR -->
	<section class="section section-muted templates-bestfor" aria-labelledby="templates-bestfor-heading">
		<div class="site-container">
			<div class="templates-bestfor__intro">
				<p class="eyebrow">Best fit</p>
				<h2 id="templates-bestfor-heading">Built for businesses that need a stronger site without a fully custom process.</h2>
			</div>
			<ul class="templates-bestfor__grid">
				{bestForCards.map((item) => (
					<li class="card templates-bestfor__card">
						<span class="templates-bestfor__check" aria-hidden="true">
							<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
						</span>
						<span>{item}</span>
					</li>
				))}
			</ul>
			<div class="templates-bestfor__note">
				<p class="templates-bestfor__note-label">Better fit for a custom build</p>
				<p>
					A Foundation may not be the right fit for complex membership portals, large associations with legacy AMS/CMS constraints, heavy custom integrations, large content libraries, or deeply custom brand systems. When that is the case, we will point you toward a
					<a href="/services/custom-healthcare-websites">Signature Custom Website</a>
					<!-- TODO: link "Web & Business Systems Consulting" once a dedicated /services/web-business-systems-consulting page exists -->
					or business systems consulting instead.
				</p>
			</div>
		</div>
	</section>

	<!-- SECTION 4 — FOUNDATION EXAMPLES / DEMOS -->
	<section id="foundation-examples" class="section templates-examples" aria-labelledby="templates-examples-heading">
		<div class="site-container site-container--wide">
			<div class="templates-examples__intro">
				<p class="eyebrow">Foundation examples</p>
				<h2 id="templates-examples-heading">Explore foundation starting points.</h2>
				<p class="lead">
					These examples are not one-size-fits-all templates. They are real, clickable starting points — structured around the pages, trust signals, and conversion paths an industry needs — that we customize around your brand, services, content, and lead path.
				</p>
			</div>

			<FeaturedFoundation slug={featuredSlug} />

			<div
				id="template-browse-toolbar"
				class="template-browse-filters"
				role="toolbar"
				aria-label="Filter Foundations"
			>
				<button type="button" class="template-browse-filters__chip is-active" data-template-filter="all" aria-pressed="true">
					All
				</button>
				{templateBrowseFilterDefs.map((chip) => (
					<button
						type="button"
						class="template-browse-filters__chip"
						data-template-filter={chip.id}
						aria-pressed="false"
					>
						{chip.label}
					</button>
				))}
			</div>
			<div id="featured-templates-grid" class="templates-examples__grid">
				{gridDemos.map((template, index) => (
					<DemoCard template={template} index={index} isNew={newDemoSlugs.has(template.slug)} />
				))}
			</div>
		</div>
	</section>

	<script is:inline>
		(() => {
			const grid = document.getElementById('featured-templates-grid');
			const toolbar = document.getElementById('template-browse-toolbar');
			if (!grid || !toolbar) return;
			const chips = toolbar.querySelectorAll('[data-template-filter]');
			const cards = grid.querySelectorAll('[data-template-card]');

			function apply(filterId) {
				chips.forEach((chip) => {
					const id = chip.getAttribute('data-template-filter') || 'all';
					const active = id === filterId;
					chip.setAttribute('aria-pressed', active ? 'true' : 'false');
					chip.classList.toggle('is-active', active);
				});

				cards.forEach((card) => {
					const raw = card.getAttribute('data-template-filters') || '';
					const tokens = raw.split(/\s+/).filter(Boolean);
					const show = filterId === 'all' || tokens.includes(filterId);
					card.hidden = !show;
				});
			}

			toolbar.addEventListener('click', (event) => {
				const chip = event.target && event.target.closest('[data-template-filter]');
				if (!chip || !toolbar.contains(chip)) return;
				apply(chip.getAttribute('data-template-filter') || 'all');
			});
		})();
	</script>

	<!-- SECTION 5 — HOW A WEBSITE FOUNDATION WORKS (unified mega-section) -->
	<section class="section section-muted templates-system" aria-labelledby="templates-system-heading">
		<div class="site-container">
			<div class="templates-system__intro">
				<p class="eyebrow">The full picture</p>
				<h2 id="templates-system-heading">Everything behind a Website Foundation.</h2>
				<p class="lead">From what you get, to how it comes together, compares, grows, and what it costs — here is the whole offer, end to end.</p>
			</div>

			<div class="fsys">
				<!-- Chapter 01 — What you get -->
				<article class="fsys__chapter">
					<div class="fsys__node" aria-hidden="true"><span>01</span></div>
					<div class="fsys__head">
						<p class="fsys__kicker">What you get</p>
						<h3 class="fsys__title">A complete, launch-ready website.</h3>
						<p class="fsys__text">You are not handed a theme to figure out. Every Foundation is built in layers, each one customized around your business.</p>
					</div>
					<div class="fsys__body fsys__body--split">
						<div class="fsys-anatomy" aria-hidden="true">
							{foundationLayers.map((layer, i) => (
								<div class={`fsys-anatomy__layer fsys-anatomy__layer--${i + 1}`}>
									<span class="fsys-anatomy__num">{String(i + 1).padStart(2, '0')}</span>
									<span class="fsys-anatomy__label">{layer}</span>
								</div>
							))}
							<p class="fsys-anatomy__base">Your Website Foundation</p>
						</div>
						<div class="fsys-include">
							{includeCategories.map((cat) => (
								<div class="fsys-include__cat">
									<h4>{cat.title}</h4>
									<ul>
										{cat.items.map((item) => (
											<li>
												<span class="fsys-check" aria-hidden="true">
													<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
												</span>
												{item}
											</li>
										))}
									</ul>
								</div>
							))}
						</div>
					</div>
					<p class="fsys__note">Final scope depends on page count, content needs, integrations, and whether a Growth System is included.</p>
				</article>

				<!-- Chapter 02 — How it comes together -->
				<article class="fsys__chapter">
					<div class="fsys__node" aria-hidden="true"><span>02</span></div>
					<div class="fsys__head">
						<p class="fsys__kicker">How it comes together</p>
						<h3 class="fsys__title">A clear, low-pressure path to launch.</h3>
						<p class="fsys__text">Six steps from first conversation to a launched site you can keep improving — starting with a free snapshot.</p>
					</div>
					<div class="fsys__body">
						<ol class="fsys-steps">
							{processSteps.map((step) => (
								<li class="fsys-steps__item">
									<span class="fsys-steps__num">{step.step}</span>
									<div class="fsys-steps__copy">
										<h4>{step.title}</h4>
										<p>{step.text}</p>
									</div>
								</li>
							))}
						</ol>
					</div>
				</article>

				<!-- Chapter 03 — How it compares -->
				<article class="fsys__chapter">
					<div class="fsys__node" aria-hidden="true"><span>03</span></div>
					<div class="fsys__head">
						<p class="fsys__kicker">How it compares</p>
						<h3 class="fsys__title">The premium middle path.</h3>
						<p class="fsys__text">A Foundation sits between a cheap template and a fully custom build: the quality and strategy of custom, with a faster, more practical path.</p>
					</div>
					<div class="fsys__body">
						<div class="fsys-scale" role="img" aria-label="A Website Foundation sits between a cheap template and a fully custom website.">
							<div class="fsys-scale__track" aria-hidden="true"></div>
							<div class="fsys-scale__stop">
								<span class="fsys-scale__dot"></span>
								<span class="fsys-scale__label">Cheap template</span>
							</div>
							<div class="fsys-scale__stop fsys-scale__stop--active">
								<span class="fsys-scale__dot">
									<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="3,8.5 6.5,12 13,4" /></svg>
								</span>
								<span class="fsys-scale__label">Website Foundation</span>
							</div>
							<div class="fsys-scale__stop">
								<span class="fsys-scale__dot"></span>
								<span class="fsys-scale__label">Fully custom</span>
							</div>
						</div>
						<div class="fsys-vs">
							{comparison.map((col) => (
								<article class={`card fsys-vs__card lane-accent-top lane-accent-top--${col.lane}`}>
									<span class={`lane-tag lane-tag--${col.lane}`}>{col.tag}</span>
									<h4>{col.name}</h4>
									<ul>
										{col.points.map((point) => (
											<li>
												<span class="fsys-check" aria-hidden="true">
													<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
												</span>
												{point}
											</li>
										))}
									</ul>
								</article>
							))}
						</div>
						<p class="fsys-vs__note">Not sure which path fits? <a href="/audit">The free snapshot makes a clear recommendation.</a></p>
					</div>
				</article>

				<!-- Chapter 04 — How it grows -->
				<article class="fsys__chapter">
					<div class="fsys__node" aria-hidden="true"><span>04</span></div>
					<div class="fsys__head">
						<p class="fsys__kicker">How it grows</p>
						<h3 class="fsys__title">Add a Growth System when your site needs more than a form.</h3>
						<p class="fsys__text">When you are ready to connect CRM, booking, follow-up, review requests, and reporting, a Growth System bolts onto your Foundation — at launch or after it is live.</p>
					</div>
					<div class="fsys__body">
						<div class="fsys-growth">
							<div class="fsys-growth__flow" aria-hidden="true">
								{growthFlow.map((node, i) => (
									<Fragment>
										<span class="fsys-growth__node">{node}</span>
										{i < growthFlow.length - 1 && <span class="fsys-growth__arrow">&rarr;</span>}
									</Fragment>
								))}
							</div>
							<ul class="fsys-growth__list">
								{growthAddItems.map((item) => (
									<li>
										<span class="fsys-check fsys-check--ink" aria-hidden="true">
											<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
										</span>
										{item}
									</li>
								))}
							</ul>
							<a class="button button-primary fsys-growth__btn" href="/services/growth-systems">Add a Growth System</a>
						</div>
					</div>
				</article>

				<!-- Chapter 05 — What it costs -->
				<article class="fsys__chapter">
					<div class="fsys__node" aria-hidden="true"><span>05</span></div>
					<div class="fsys__head">
						<p class="fsys__kicker">What it costs</p>
						<h3 class="fsys__title">Website Foundations start at $3,500.</h3>
						<p class="fsys__text">Final scope depends on page count, content needs, integrations, and whether a Growth System or ongoing management plan is included.</p>
					</div>
					<div class="fsys__body">
						<div class="fsys-pricing">
							{pricingTiers.map((tier) => (
								<article class:list={['card', 'fsys-pricing__card', { 'is-featured': tier.featured }]}>
									{tier.featured && <span class="fsys-pricing__badge">Most popular</span>}
									<h4>{tier.name}</h4>
									<p class="fsys-pricing__price">{tier.price}</p>
									<p class="fsys-pricing__text">{tier.text}</p>
								</article>
							))}
						</div>
					</div>
				</article>

				<!-- Spine terminus CTA -->
				<div class="fsys__end">
					<div class="fsys__node fsys__node--end" aria-hidden="true">
						<svg viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="3,8.5 6.5,12 13,4" /></svg>
					</div>
					<div class="fsys__end-card">
						<p>Every path starts the same way: a free, no-pressure snapshot with a clear recommendation.</p>
						<a class="button button-primary" href="/audit">Get My Free Snapshot</a>
					</div>
				</div>
			</div>
		</div>
	</section>

	<!-- SECTION 10 — FAQ -->
	<ServiceFAQSection faqs={foundationFaqs} />

	<!-- SECTION 11 — FINAL CTA -->
	<section class="section templates-final" aria-labelledby="templates-final-heading">
		<div class="site-container">
			<div class="templates-final__card">
				<div class="templates-final__copy">
					<p class="eyebrow">Start with a free snapshot</p>
					<h2 id="templates-final-heading">Not sure if a Website Foundation fits?</h2>
					<p class="lead">
						I'll review your site, lead path, and follow-up flow, then send a short Loom with the clearest opportunities and the best starting point.
					</p>
					<p class="templates-final__micro">Free Loom review · Clear recommendation · No pressure</p>
				</div>
				<div class="button-row">
					<a class="button button-primary" href="/audit">Get My Free Snapshot</a>
					<a class="button button-secondary" href="#foundation-examples">Explore Foundation Examples</a>
				</div>
			</div>
		</div>
	</section>
</BaseLayout>

<style>
	/* ═══ Hero ═══ */
	.templates-hero {
		position: relative;
		overflow: hidden;
		padding-block: clamp(5rem, 8vw, 8rem);
	}

	.templates-hero::before {
		position: absolute;
		inset: 0;
		z-index: -1;
		background:
			radial-gradient(circle at 76% 14%, rgba(95, 143, 139, 0.12), transparent 30%),
			linear-gradient(180deg, var(--color-white) 0%, var(--color-bg) 72%);
		content: '';
	}

	.templates-bestfor__intro,
	.templates-examples__intro,
	.templates-system__intro {
		max-width: 820px;
	}

	.templates-hero__grid {
		display: grid;
		grid-template-columns: minmax(0, 1.02fr) minmax(0, 0.98fr);
		gap: clamp(2.5rem, 5vw, 4.5rem);
		align-items: center;
	}

	.templates-hero__copy {
		max-width: 560px;
	}

	.templates-hero h1 {
		font-size: clamp(2.3rem, 3.7vw, 3.2rem);
		letter-spacing: -0.03em;
		line-height: 1.05;
	}

	.templates-hero .lead {
		margin-top: 1.4rem;
	}

	.templates-hero__chips {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
		margin: 1.75rem 0 0;
		padding: 0;
		list-style: none;
	}

	.templates-hero__chips li {
		display: inline-flex;
		align-items: center;
		padding: 0.42rem 0.85rem;
		border: 1px solid color-mix(in srgb, var(--color-accent) 26%, var(--color-border));
		border-radius: 999px;
		background: var(--color-white);
		color: var(--color-accent-dark);
		font-size: 0.84rem;
		font-weight: 700;
		line-height: 1;
	}

	.templates-hero__chips li.is-accent {
		border-color: transparent;
		background: var(--color-accent-dark);
		color: var(--color-white);
	}

	.templates-hero .button-row {
		margin-top: 2rem;
	}

	.templates-hero__micro {
		margin: 1.05rem 0 0;
		color: var(--color-muted);
		font-size: 0.85rem;
		font-weight: 600;
		letter-spacing: 0.01em;
	}

	/* ═══ Hero visual — Foundation Preview ═══ */
	.templates-hero__media {
		display: flex;
		justify-content: center;
	}

	.foundation-visual {
		position: relative;
		width: min(100%, 440px);
		padding: 2.25rem 1.75rem;
	}

	/* decorative layered cards behind the preview = "foundation" depth */
	.fv-deco {
		position: absolute;
		inset: 2.25rem 1.75rem;
		border-radius: 16px;
		background: var(--color-white);
		border: 1px solid var(--color-border-soft);
	}

	.fv-deco--back {
		transform: translate(22px, -26px) rotate(2.4deg);
		opacity: 0.55;
		box-shadow: var(--shadow-card);
	}

	.fv-deco--mid {
		transform: translate(11px, -13px) rotate(1.2deg);
		opacity: 0.8;
		box-shadow: var(--shadow-card);
	}

	.fv-browser {
		position: relative;
		border-radius: 16px;
		overflow: hidden;
		background: var(--color-white);
		border: 1px solid var(--color-border);
		box-shadow: var(--shadow-card-hover);
	}

	.fv-browser__bar {
		display: flex;
		align-items: center;
		gap: 0.4rem;
		padding: 0.6rem 0.85rem;
		background: color-mix(in srgb, var(--color-bg) 65%, var(--color-white));
		border-bottom: 1px solid var(--color-border-soft);
	}

	.fv-browser__dot {
		width: 0.5rem;
		height: 0.5rem;
		border-radius: 999px;
		background: var(--color-border);
	}

	.fv-browser__url {
		margin-left: 0.5rem;
		padding: 0.18rem 0.6rem;
		border-radius: 999px;
		background: var(--color-white);
		border: 1px solid var(--color-border-soft);
		color: var(--color-muted);
		font-size: 0.6rem;
		font-weight: 600;
		letter-spacing: 0.01em;
	}

	.fv-screen {
		display: grid;
		gap: 0.85rem;
		padding: 1.1rem;
	}

	.fv-screen__hero {
		display: grid;
		gap: 0.5rem;
		padding: 1rem;
		border-radius: 12px;
		background:
			radial-gradient(circle at 88% 8%, color-mix(in srgb, var(--color-accent) 16%, transparent), transparent 42%),
			color-mix(in srgb, var(--color-accent-light) 55%, var(--color-white));
		border: 1px solid var(--color-border-soft);
	}

	.fv-pill {
		justify-self: start;
		padding: 0.22rem 0.55rem;
		border-radius: 999px;
		background: var(--color-white);
		color: var(--color-accent-dark);
		font-size: 0.58rem;
		font-weight: 800;
		letter-spacing: 0.04em;
		text-transform: uppercase;
	}

	.fv-bar {
		display: block;
		height: 0.55rem;
		border-radius: 999px;
		background: color-mix(in srgb, var(--color-muted) 22%, var(--color-border-soft));
	}

	.fv-bar--title {
		height: 0.72rem;
		width: 82%;
		background: color-mix(in srgb, var(--color-text) 70%, var(--color-border));
	}

	.fv-bar--title-2 {
		width: 58%;
	}

	.fv-bar--text {
		width: 70%;
		height: 0.42rem;
		margin-top: 0.1rem;
	}

	.fv-screen__cta {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		margin-top: 0.35rem;
	}

	.fv-btn {
		padding: 0.4rem 0.7rem;
		border-radius: 8px;
		background: var(--color-text);
		color: var(--color-white);
		font-size: 0.62rem;
		font-weight: 700;
	}

	.fv-tag {
		padding: 0.28rem 0.5rem;
		border-radius: 999px;
		background: var(--color-white);
		border: 1px dashed color-mix(in srgb, var(--color-accent) 45%, var(--color-border));
		color: var(--color-accent-dark);
		font-size: 0.55rem;
		font-weight: 700;
	}

	.fv-services {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 0.6rem;
	}

	.fv-card {
		display: grid;
		gap: 0.35rem;
		padding: 0.65rem;
		border-radius: 10px;
		background: var(--color-white);
		border: 1px solid var(--color-border-soft);
	}

	.fv-card__icon {
		width: 1.15rem;
		height: 1.15rem;
		border-radius: 7px;
		background: var(--color-accent-light);
		border: 1px solid color-mix(in srgb, var(--color-accent) 32%, var(--color-border));
	}

	.fv-bar--sm {
		width: 90%;
		height: 0.42rem;
	}

	.fv-bar--xs {
		width: 60%;
		height: 0.35rem;
	}

	.fv-trust {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		padding: 0.6rem 0.75rem;
		border-radius: 10px;
		background: color-mix(in srgb, var(--color-bg) 60%, var(--color-white));
		border: 1px solid var(--color-border-soft);
	}

	.fv-stars {
		color: var(--color-accent-dark);
		font-size: 0.72rem;
		letter-spacing: 0.06em;
	}

	.fv-bar--trust {
		flex: 1;
		width: auto;
		height: 0.4rem;
	}

	/* floating strategic callouts annotating the less-visible foundation layers */
	.fv-callout {
		position: absolute;
		display: inline-flex;
		align-items: center;
		gap: 0.35rem;
		padding: 0.4rem 0.7rem;
		border-radius: 999px;
		background: var(--color-white);
		border: 1px solid var(--color-border);
		color: var(--color-text);
		font-size: 0.72rem;
		font-weight: 700;
		white-space: nowrap;
		box-shadow: var(--shadow-card);
	}

	.fv-callout::before {
		content: '';
		width: 0.42rem;
		height: 0.42rem;
		border-radius: 999px;
		background: var(--color-accent-dark);
	}

	.fv-callout--lead {
		top: 0.6rem;
		right: -0.4rem;
	}

	.fv-callout--edit {
		bottom: 3.1rem;
		left: -0.6rem;
	}

	.fv-callout--growth {
		bottom: 0.4rem;
		right: 0.6rem;
	}

	@media (prefers-reduced-motion: no-preference) {
		.foundation-visual {
			animation: fv-rise 0.7s ease both;
		}
	}

	@keyframes fv-rise {
		from {
			opacity: 0;
			transform: translateY(12px);
		}
		to {
			opacity: 1;
			transform: translateY(0);
		}
	}

	/* ═══ Define / not-this-this ═══ */
	.templates-define__inner {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
		gap: clamp(2rem, 5vw, 4rem);
		align-items: center;
	}

	.templates-define__copy .lead {
		font-size: clamp(1.12rem, 1.7vw, 1.3rem);
	}

	.templates-define__support {
		margin-top: 1.1rem;
		color: var(--color-muted);
		font-size: 0.975rem;
		line-height: 1.6;
		max-width: 46ch;
	}

	.templates-define__compare-label {
		margin: 0 0 0.9rem;
		font-size: 0.74rem;
		font-weight: 800;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--color-muted);
	}

	.templates-define__cards {
		display: grid;
		grid-template-columns: 1fr;
		gap: 1.1rem;
	}

	.templates-define__col {
		position: relative;
		overflow: hidden;
		padding: 1.5rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-white);
		box-shadow: var(--shadow-card);
	}

	/* top accent bar distinguishes avoid vs recommended */
	.templates-define__col::before {
		content: '';
		position: absolute;
		inset: 0 0 auto 0;
		height: 3px;
	}

	.templates-define__col--not {
		background: color-mix(in srgb, var(--color-bg) 72%, var(--color-white));
		border-color: var(--color-border-soft);
	}

	.templates-define__col--not::before {
		background: color-mix(in srgb, var(--color-muted) 28%, var(--color-border));
	}

	.templates-define__col--yes {
		background:
			linear-gradient(180deg, color-mix(in srgb, var(--color-accent-light) 60%, var(--color-white)) 0%, var(--color-white) 60%);
		border-color: color-mix(in srgb, var(--color-accent) 42%, var(--color-border));
		box-shadow: 0 0 0 1px color-mix(in srgb, var(--color-accent) 18%, transparent), var(--shadow-card-hover);
	}

	.templates-define__col--yes::before {
		background: var(--color-accent-dark);
	}

	.templates-define__col-label {
		display: flex;
		align-items: center;
		gap: 0.55rem;
		margin: 0 0 0.85rem;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-muted);
	}

	.templates-define__col--yes .templates-define__col-label {
		color: var(--color-accent-dark);
	}

	.templates-define__col-badge {
		padding: 0.22rem 0.5rem;
		border-radius: 999px;
		background: var(--color-accent-dark);
		color: var(--color-white);
		font-size: 0.6rem;
		font-weight: 800;
		letter-spacing: 0.06em;
	}

	.templates-define__col ul {
		display: grid;
		gap: 0.7rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.templates-define__col li {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: start;
		font-size: 0.9375rem;
		line-height: 1.45;
	}

	.templates-define__col--not li {
		color: var(--color-muted);
	}

	.templates-define__col--yes li {
		color: var(--color-text);
		font-weight: 500;
	}

	.templates-define__icon {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		width: 1.4rem;
		height: 1.4rem;
		margin-top: 0.02rem;
		border-radius: 999px;
	}

	.templates-define__icon svg {
		width: 0.8rem;
		height: 0.8rem;
	}

	.templates-define__icon--x {
		background: color-mix(in srgb, var(--color-muted) 14%, transparent);
		color: var(--color-muted);
	}

	.templates-define__icon--check {
		background: var(--color-accent-dark);
		color: var(--color-white);
	}

	/* ═══ Best for ═══ */
	.templates-bestfor__grid {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
		margin: clamp(2rem, 4vw, 3rem) 0 0;
		padding: 0;
		list-style: none;
	}

	.templates-bestfor__card {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.7rem;
		align-items: center;
		padding: 1.15rem 1.25rem;
		font-size: 0.95rem;
		font-weight: 600;
		color: var(--color-text);
		line-height: 1.4;
	}

	.templates-bestfor__check {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		width: 1.5rem;
		height: 1.5rem;
		border-radius: 999px;
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
	}

	.templates-bestfor__check svg {
		width: 0.85rem;
		height: 0.85rem;
	}

	.templates-bestfor__note {
		margin-top: 1.5rem;
		padding: 1.35rem 1.5rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-white);
	}

	.templates-bestfor__note-label {
		margin: 0 0 0.4rem;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-muted);
	}

	.templates-bestfor__note p:last-child {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.95rem;
		line-height: 1.6;
	}

	.templates-bestfor__note a {
		color: var(--color-accent-dark);
		font-weight: 700;
		text-decoration: underline;
		text-underline-offset: 2px;
	}

	/* ═══ Examples ═══ */
	.templates-examples__grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.5rem;
		margin-top: clamp(2rem, 4vw, 3rem);
	}

	.template-browse-filters {
		display: flex;
		flex-wrap: wrap;
		gap: 0.6rem;
		margin-top: 1.75rem;
	}

	.template-browse-filters__chip {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 2.25rem;
		padding: 0.42rem 0.82rem;
		border: 1px solid var(--color-border);
		border-radius: 999px;
		background: var(--color-white);
		color: var(--color-muted);
		font-family: inherit;
		font-size: 0.8125rem;
		font-weight: 700;
		line-height: 1;
		cursor: pointer;
		transition: border-color 0.12s ease, background-color 0.12s ease, color 0.12s ease;
	}

	.template-browse-filters__chip:hover {
		border-color: color-mix(in srgb, var(--color-accent) 38%, var(--color-border));
		color: var(--color-accent-dark);
		background: color-mix(in srgb, var(--color-accent-light) 76%, var(--color-white));
	}

	.template-browse-filters__chip:focus-visible {
		outline: 2px solid var(--color-accent-dark);
		outline-offset: 2px;
	}

	.template-browse-filters__chip.is-active {
		border-color: color-mix(in srgb, var(--color-accent) 48%, var(--color-border));
		background: color-mix(in srgb, var(--color-accent-light) 88%, var(--color-white));
		color: var(--color-accent-dark);
		box-shadow: 0 0 0 1px color-mix(in srgb, var(--color-accent) 22%, transparent);
	}

	/* ═══ Mega-section: How a Website Foundation works ═══ */
	.templates-system__intro {
		margin-bottom: clamp(2.5rem, 5vw, 3.75rem);
	}

	.fsys {
		position: relative;
		display: grid;
		gap: clamp(2.75rem, 5vw, 4.25rem);
	}

	/* the connecting spine */
	.fsys::before {
		content: '';
		position: absolute;
		top: 1rem;
		bottom: 3rem;
		left: 1.4rem;
		width: 2px;
		background: linear-gradient(
			180deg,
			var(--color-accent) 0%,
			color-mix(in srgb, var(--color-accent) 55%, var(--color-border)) 60%,
			var(--color-border) 100%
		);
		z-index: 0;
	}

	.fsys__chapter {
		position: relative;
		padding-left: clamp(3.5rem, 5vw, 4.75rem);
	}

	.fsys__node {
		position: absolute;
		left: 0;
		top: 0;
		z-index: 1;
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 2.8rem;
		height: 2.8rem;
		border-radius: 999px;
		background: var(--color-white);
		border: 1px solid color-mix(in srgb, var(--color-accent) 42%, var(--color-border));
		box-shadow: var(--shadow-card);
	}

	.fsys__node span {
		color: var(--color-accent-dark);
		font-size: 0.95rem;
		font-weight: 800;
		letter-spacing: -0.02em;
	}

	.fsys__head {
		max-width: 64ch;
		margin-bottom: 1.6rem;
	}

	.fsys__kicker {
		margin: 0 0 0.5rem;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.12em;
		text-transform: uppercase;
		color: var(--color-accent-dark);
	}

	.fsys__title {
		font-size: clamp(1.4rem, 2.2vw, 1.85rem);
		letter-spacing: -0.025em;
		line-height: 1.15;
	}

	.fsys__text {
		margin: 0.6rem 0 0;
		max-width: 56ch;
		color: var(--color-muted);
		font-size: 1rem;
		line-height: 1.6;
	}

	.fsys__note {
		margin: 1.25rem 0 0;
		color: var(--color-muted);
		font-size: 0.9rem;
		font-style: italic;
	}

	.fsys__body--split {
		display: grid;
		grid-template-columns: minmax(0, 0.82fr) minmax(0, 1.18fr);
		gap: clamp(1.5rem, 3vw, 2.5rem);
		align-items: start;
	}

	.fsys-check {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		flex-shrink: 0;
		width: 1.2rem;
		height: 1.2rem;
		margin-top: 0.05rem;
		border-radius: 999px;
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
	}

	.fsys-check svg {
		width: 0.72rem;
		height: 0.72rem;
	}

	.fsys-check--ink {
		background: var(--color-ink-accent);
		color: var(--color-ink);
	}

	/* Chapter 01 — anatomy stack */
	.fsys-anatomy {
		display: grid;
		gap: 0.5rem;
		padding: 1.25rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: linear-gradient(180deg, color-mix(in srgb, var(--color-accent-light) 45%, var(--color-white)), var(--color-white));
		box-shadow: var(--shadow-card);
	}

	.fsys-anatomy__layer {
		display: flex;
		align-items: center;
		gap: 0.7rem;
		padding: 0.7rem 0.85rem;
		border: 1px solid var(--color-border-soft);
		border-left: 3px solid color-mix(in srgb, var(--color-accent) 55%, var(--color-border));
		border-radius: 10px;
		background: var(--color-white);
		box-shadow: 0 1px 2px rgba(15, 23, 42, 0.04);
	}

	.fsys-anatomy__num {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 1.5rem;
		height: 1.5rem;
		border-radius: 6px;
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
		font-size: 0.6rem;
		font-weight: 800;
	}

	.fsys-anatomy__label {
		font-size: 0.9rem;
		font-weight: 700;
		color: var(--color-text);
	}

	.fsys-anatomy__base {
		margin: 0.45rem 0 0;
		text-align: center;
		font-size: 0.72rem;
		font-weight: 800;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		color: var(--color-accent-dark);
	}

	/* Chapter 01 — include detail */
	.fsys-include {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.1rem 1.5rem;
	}

	.fsys-include__cat h4 {
		margin: 0 0 0.5rem;
		font-size: 0.95rem;
		letter-spacing: -0.01em;
	}

	.fsys-include__cat ul {
		display: grid;
		gap: 0.42rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.fsys-include__cat li {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.5rem;
		align-items: start;
		color: var(--color-muted);
		font-size: 0.85rem;
		line-height: 1.4;
	}

	/* Chapter 02 — steps */
	.fsys-steps {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.fsys-steps__item {
		display: grid;
		gap: 0.5rem;
		align-content: start;
		padding: 1.35rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-white);
		box-shadow: var(--shadow-card);
	}

	.fsys-steps__num {
		display: inline-flex;
		width: fit-content;
		margin-bottom: 0.4rem;
		padding: 0.3rem 0.5rem;
		border-radius: 999px;
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
		font-size: 0.7rem;
		font-weight: 800;
		line-height: 1;
	}

	.fsys-steps__copy h4 {
		margin: 0 0 0.25rem;
		font-size: 1rem;
		letter-spacing: -0.01em;
	}

	.fsys-steps__copy p {
		margin: 0;
		color: var(--color-muted);
		font-size: 0.9rem;
		line-height: 1.55;
	}

	/* Chapter 03 — middle-path scale */
	.fsys-scale {
		position: relative;
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 1rem;
		margin-bottom: 1.75rem;
		padding: 1.5rem 1.25rem 1.25rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: var(--color-white);
		box-shadow: var(--shadow-card);
	}

	.fsys-scale__track {
		position: absolute;
		top: 2.3rem;
		left: 16.6%;
		right: 16.6%;
		height: 2px;
		background: linear-gradient(90deg, var(--color-border), var(--color-accent), var(--color-border));
	}

	.fsys-scale__stop {
		position: relative;
		z-index: 1;
		display: grid;
		justify-items: center;
		gap: 0.65rem;
		text-align: center;
	}

	.fsys-scale__dot {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		width: 1.6rem;
		height: 1.6rem;
		border-radius: 999px;
		background: var(--color-white);
		border: 2px solid var(--color-border);
		color: transparent;
	}

	.fsys-scale__stop--active .fsys-scale__dot {
		background: var(--color-accent-dark);
		border-color: var(--color-accent-dark);
		color: var(--color-white);
		box-shadow: 0 0 0 5px color-mix(in srgb, var(--color-accent) 20%, transparent);
	}

	.fsys-scale__dot svg {
		width: 0.85rem;
		height: 0.85rem;
	}

	.fsys-scale__label {
		font-size: 0.8rem;
		font-weight: 700;
		color: var(--color-muted);
	}

	.fsys-scale__stop--active .fsys-scale__label {
		color: var(--color-text);
		font-weight: 800;
	}

	/* Chapter 03 — comparison cards */
	.fsys-vs {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 1.25rem;
	}

	.fsys-vs__card {
		display: grid;
		gap: 0.85rem;
		align-content: start;
		padding: 1.75rem;
	}

	.fsys-vs__card h4 {
		font-size: 1.15rem;
		letter-spacing: -0.02em;
	}

	.fsys-vs__card ul {
		display: grid;
		gap: 0.7rem;
		margin: 0.4rem 0 0;
		padding: 0;
		list-style: none;
	}

	.fsys-vs__card li {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: start;
		color: var(--color-muted);
		font-size: 0.9375rem;
		line-height: 1.5;
	}

	.fsys-vs__note {
		margin: 1.5rem 0 0;
		color: var(--color-muted);
		font-size: 0.95rem;
	}

	.fsys-vs__note a {
		color: var(--color-accent-dark);
		font-weight: 700;
		text-decoration: underline;
		text-underline-offset: 2px;
	}

	/* Chapter 04 — growth (dark) */
	.fsys-growth {
		display: grid;
		gap: 1.5rem;
		padding: clamp(1.75rem, 4vw, 2.5rem);
		border: 1px solid var(--color-ink-line-strong);
		border-radius: 20px;
		background: var(--color-ink);
		box-shadow: 0 24px 60px rgba(15, 23, 42, 0.18);
	}

	.fsys-growth__flow {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 0.55rem;
	}

	.fsys-growth__node {
		padding: 0.45rem 0.8rem;
		border-radius: 999px;
		background: color-mix(in srgb, var(--color-ink-accent) 16%, transparent);
		border: 1px solid var(--color-ink-line-strong);
		color: var(--color-white);
		font-size: 0.82rem;
		font-weight: 700;
	}

	.fsys-growth__arrow {
		color: var(--color-ink-accent);
		font-weight: 800;
	}

	.fsys-growth__list {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.7rem 1.25rem;
		margin: 0;
		padding: 0;
		list-style: none;
	}

	.fsys-growth__list li {
		display: grid;
		grid-template-columns: auto 1fr;
		gap: 0.6rem;
		align-items: center;
		color: var(--color-ink-text);
		font-size: 0.92rem;
		line-height: 1.4;
	}

	.fsys-growth__btn {
		justify-self: start;
	}

	/* Chapter 05 — pricing */
	.fsys-pricing {
		display: grid;
		grid-template-columns: repeat(3, minmax(0, 1fr));
		gap: 1.25rem;
	}

	.fsys-pricing__card {
		position: relative;
		display: grid;
		gap: 0.5rem;
		align-content: start;
		padding: 1.75rem;
	}

	.fsys-pricing__card.is-featured {
		border-color: color-mix(in srgb, var(--color-accent) 50%, var(--color-border));
		box-shadow: 0 0 0 1px color-mix(in srgb, var(--color-accent) 26%, transparent), var(--shadow-card);
	}

	.fsys-pricing__badge {
		position: absolute;
		top: -0.7rem;
		left: 1.75rem;
		padding: 0.3rem 0.6rem;
		border-radius: 999px;
		background: var(--color-accent-dark);
		color: var(--color-white);
		font-size: 0.68rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.fsys-pricing__card h4 {
		font-size: 1.1rem;
		letter-spacing: -0.02em;
	}

	.fsys-pricing__price {
		margin: 0;
		color: var(--color-accent-dark);
		font-size: 1.15rem;
		font-weight: 800;
		letter-spacing: -0.01em;
	}

	.fsys-pricing__text {
		margin: 0.25rem 0 0;
		color: var(--color-muted);
		font-size: 0.9375rem;
		line-height: 1.55;
	}

	/* spine terminus */
	.fsys__end {
		position: relative;
		padding-left: clamp(3.5rem, 5vw, 4.75rem);
	}

	.fsys__node--end {
		background: var(--color-accent-dark);
		border-color: var(--color-accent-dark);
		color: var(--color-white);
	}

	.fsys__node--end svg {
		width: 1.1rem;
		height: 1.1rem;
	}

	.fsys__end-card {
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		gap: 1.1rem 2rem;
		padding: 1.5rem 1.75rem;
		border: 1px solid var(--color-border);
		border-radius: var(--radius-lg);
		background: linear-gradient(180deg, color-mix(in srgb, var(--color-accent-light) 50%, var(--color-white)), var(--color-white));
		box-shadow: var(--shadow-card);
	}

	.fsys__end-card p {
		flex: 1;
		min-width: 16rem;
		margin: 0;
		color: var(--color-text);
		font-size: 1rem;
		font-weight: 600;
		line-height: 1.5;
	}

	/* ═══ Final CTA ═══ */
	.templates-final__card {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		gap: 2rem;
		align-items: end;
		padding: clamp(1.75rem, 4vw, 3rem);
		border-radius: 20px;
		background: var(--color-text);
		box-shadow: 0 24px 60px rgba(15, 23, 42, 0.16);
	}

	.templates-final .eyebrow {
		color: color-mix(in srgb, var(--color-accent-light) 84%, var(--color-white));
	}

	.templates-final h2,
	.templates-final .lead {
		color: var(--color-white);
	}

	.templates-final .lead {
		opacity: 0.8;
	}

	.templates-final__micro {
		margin: 0.85rem 0 0;
		color: color-mix(in srgb, var(--color-accent-light) 80%, var(--color-white));
		font-size: 0.85rem;
		font-weight: 600;
	}

	.templates-final .button-row {
		justify-content: flex-end;
		margin-top: 0;
	}

	.templates-final .button-secondary {
		background: transparent;
		border-color: rgba(255, 255, 255, 0.22);
		color: var(--color-white);
	}

	.templates-final .button-secondary:hover {
		background: rgba(255, 255, 255, 0.08);
		border-color: rgba(255, 255, 255, 0.38);
		color: var(--color-white);
	}

	/* ═══ Responsive ═══ */
	@media (max-width: 1040px) {
		.templates-hero__grid {
			grid-template-columns: 1fr;
			gap: clamp(2.5rem, 7vw, 3.25rem);
		}

		.templates-hero__copy {
			max-width: 640px;
		}

		.templates-hero__media {
			order: 2;
		}

		.templates-define__inner {
			grid-template-columns: 1fr;
		}

		.templates-bestfor__grid,
		.templates-examples__grid {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}

		.fsys__body--split {
			grid-template-columns: 1fr;
		}

		.fsys-steps {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 720px) {
		.templates-bestfor__grid,
		.templates-examples__grid,
		.templates-final__card,
		.fsys-include,
		.fsys-steps,
		.fsys-vs,
		.fsys-growth__list,
		.fsys-pricing {
			grid-template-columns: 1fr;
		}

		.templates-final .button-row {
			justify-content: flex-start;
		}

		/* Tighten the spine + chapters on small screens */
		.fsys::before {
			left: 1rem;
		}

		.fsys__chapter,
		.fsys__end {
			padding-left: 2.75rem;
		}

		.fsys__node {
			width: 2rem;
			height: 2rem;
		}

		.fsys__node span {
			font-size: 0.78rem;
		}

		/* Tighten the hero visual + reduce callouts on small screens */
		.foundation-visual {
			width: min(100%, 380px);
			padding: 1.75rem 1.4rem;
		}

		.fv-callout--edit {
			display: none;
		}

		.fv-callout--lead {
			right: 0;
		}
	}
</style>
