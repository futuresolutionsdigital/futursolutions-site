---
import DemoLayout from '../../../layouts/DemoLayout.astro';

const treatments = [
	{
		title: 'Injectables',
		text: 'Consultation-led aesthetic treatments focused on balance, facial harmony, and natural-looking refinement.',
	},
	{
		title: 'Medical Weight Loss',
		text: 'A structured wellness pathway for clients looking for guided support, accountability, and clearer next steps.',
	},
	{
		title: 'Hormone Support',
		text: 'Education-first consultations for clients exploring symptoms, options, and wellness goals.',
	},
	{
		title: 'IV Therapy',
		text: 'Wellness-focused hydration services presented with clear expectations and simple booking prompts.',
	},
];

const trustCards = ['Thoughtful consultations', 'Clear treatment education', 'Calm clinical environment', 'Simple booking path'];
const flowSteps = ['Treatment interest', 'Consultation request', 'Intake questions', 'Follow-up'];
---

<DemoLayout
	title="Luma Aesthetics Demo | Future Solutions"
	description="Luma Aesthetics offers consultation-led aesthetic and wellness care with clear education and a calm, personalized experience."
	canonicalUrl="https://futursolutions.vercel.app/demos/med-spa/"
>
	<section class="luma-hero" aria-labelledby="luma-heading">
		<div class="site-container luma-hero__inner">
			<div>
				<p class="luma-eyebrow">Aesthetic &amp; Wellness</p>
				<h1 id="luma-heading">Modern aesthetic care with a calm, clinical touch.</h1>
				<p class="luma-lead">
					Luma Aesthetics helps clients explore aesthetic and wellness treatments through clear education, thoughtful consultations, and a calm, personalized experience.
				</p>
				<div class="luma-button-row">
					<a class="luma-button luma-button--primary" href="/demos/med-spa/contact">Book Consultation</a>
					<a class="luma-button luma-button--secondary" href="/demos/med-spa/treatments">View Treatments</a>
				</div>
				<p class="luma-trust-line">A Future Solutions demo template for med spas and treatment-based wellness clinics.</p>
			</div>
			<div class="luma-hero__panel" aria-hidden="true">
				<div class="luma-hero__card">
					<span>Consultation-ready</span>
					<strong>Education first. Questions welcome.</strong>
					<p>Learn about options · Speak with the team · Plan your next step</p>
				</div>
				<div class="luma-hero__metric">
					<span>Calm pacing, attentive care.</span>
				</div>
			</div>
		</div>
	</section>

	<section class="luma-section luma-section--muted" aria-labelledby="treatment-preview-heading">
		<div class="site-container">
			<div class="luma-section__intro">
				<p class="luma-eyebrow">Services</p>
				<h2 id="treatment-preview-heading">Care categories with room to learn and ask questions.</h2>
			</div>
			<div class="luma-grid luma-grid--4">
				{treatments.map((treatment) => (
					<article class="luma-card">
						<h3>{treatment.title}</h3>
						<p>{treatment.text}</p>
					</article>
				))}
			</div>
			<a class="luma-link-cta" href="/demos/med-spa/treatments">View All Treatments</a>
		</div>
	</section>

	<section class="luma-section" aria-labelledby="trust-heading">
		<div class="site-container">
			<div class="luma-section__intro">
				<p class="luma-eyebrow">What to expect</p>
				<h2 id="trust-heading">Clear information and a steadier pace from first visit to consultation.</h2>
			</div>
			<div class="luma-grid luma-grid--4">
				{trustCards.map((card) => (
					<article class="luma-card luma-card--compact">
						<span aria-hidden="true"></span>
						<h3>{card}</h3>
					</article>
				))}
			</div>
		</div>
	</section>

	<section id="about" class="luma-section luma-about" aria-labelledby="about-heading">
		<div class="site-container luma-about__card">
			<p class="luma-eyebrow">About</p>
			<h2 id="about-heading">A calmer approach to aesthetic care.</h2>
			<p class="luma-lead">
				Luma Aesthetics is built around a calmer approach to aesthetic care: clear information, thoughtful recommendations, and a consultation process designed to help clients feel informed before taking the next step.
			</p>
		</div>
	</section>

	<section class="luma-section luma-section--muted" aria-labelledby="conversion-heading">
		<div class="site-container">
			<div class="luma-section__intro">
				<p class="luma-eyebrow">Consultation journey</p>
				<h2 id="conversion-heading">A simple path when you are ready to connect.</h2>
				<p class="luma-lead">
					When you find a service that fits your goals, you can request a consultation, share a few details, and hear back with next steps—without pressure or clutter.
				</p>
			</div>
			<div class="luma-flow" aria-label="Luma Aesthetics consultation journey">
				{flowSteps.map((step, index) => (
					<div class="luma-flow__step">
						<span>{String(index + 1).padStart(2, '0')}</span>
						<p>{step}</p>
					</div>
				))}
			</div>
		</div>
	</section>

	<section class="luma-section luma-final" aria-labelledby="final-heading">
		<div class="site-container luma-final__card">
			<div>
				<p class="luma-eyebrow">Future Solutions template</p>
				<h2 id="final-heading">Want this med spa system customized for your brand?</h2>
				<p class="luma-lead">
					This demo can be adapted around your services, brand, local visibility goals, and backend lead system.
				</p>
			</div>
			<div class="luma-button-row">
				<a class="luma-button luma-button--primary" href="/audit?template=med-spa">Get This Template Customized</a>
				<a class="luma-button luma-button--secondary" href="/templates/med-spa">Back to Template Details</a>
			</div>
		</div>
	</section>
</DemoLayout>

<style>
	.luma-hero,
	.luma-section {
		padding-block: clamp(4.5rem, 8vw, 7rem);
	}

	.luma-hero {
		background:
			radial-gradient(circle at 82% 12%, rgba(143, 111, 99, 0.16), transparent 30%),
			linear-gradient(180deg, #ffffff 0%, #fbfaf7 78%);
	}

	.luma-hero__inner {
		display: grid;
		grid-template-columns: minmax(0, 1fr) minmax(340px, 0.72fr);
		gap: clamp(3rem, 6vw, 5rem);
		align-items: center;
	}

	.luma-eyebrow {
		margin-bottom: 1rem;
		color: #8f6f63;
		font-size: 0.8125rem;
		font-weight: 800;
		letter-spacing: 0.1em;
		text-transform: uppercase;
	}

	.luma-hero h1,
	.luma-section h2 {
		color: #17202a;
	}

	.luma-hero h1 {
		max-width: 11.5ch;
		font-size: clamp(3.1rem, 2.4rem + 3vw, 4.6rem);
	}

	.luma-lead {
		max-width: 700px;
		color: #64706f;
		font-size: clamp(1.08rem, 1rem + 0.32vw, 1.25rem);
		line-height: 1.65;
	}

	.luma-button-row {
		display: flex;
		flex-wrap: wrap;
		gap: 0.875rem;
		margin-top: 2rem;
	}

	.luma-button {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 2.875rem;
		padding: 0.8rem 1.1rem;
		border: 1px solid transparent;
		border-radius: 999px;
		font-size: 0.9375rem;
		font-weight: 800;
		line-height: 1;
	}

	.luma-button--primary {
		background: #8f6f63;
		color: #ffffff;
	}

	.luma-button--primary:hover {
		background: #6f554b;
		color: #ffffff;
	}

	.luma-button--secondary {
		border-color: rgba(42, 54, 65, 0.12);
		background: #ffffff;
		color: #17202a;
	}

	.luma-button--secondary:hover {
		background: #efe7e1;
		color: #6f554b;
	}

	.luma-trust-line {
		max-width: 620px;
		margin-top: 1.25rem;
		color: #64706f;
		font-size: 0.9375rem;
	}

	.luma-hero__panel {
		display: grid;
		gap: 1rem;
	}

	.luma-hero__card,
	.luma-hero__metric,
	.luma-card,
	.luma-flow__step {
		border: 1px solid rgba(42, 54, 65, 0.1);
		border-radius: 22px;
		background: #ffffff;
		box-shadow: 0 20px 52px rgba(42, 54, 65, 0.05);
	}

	.luma-hero__card {
		min-height: 320px;
		padding: 1.5rem;
		background:
			linear-gradient(135deg, rgba(239, 231, 225, 0.84), rgba(255, 255, 255, 0.9)),
			#ffffff;
	}

	.luma-hero__card span,
	.luma-hero__metric span {
		color: #8f6f63;
		font-size: 0.8rem;
		font-weight: 800;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.luma-hero__card strong {
		display: block;
		max-width: 280px;
		margin-top: 8rem;
		color: #17202a;
		font-size: 1.7rem;
		letter-spacing: -0.04em;
		line-height: 1.1;
	}

	.luma-hero__card p {
		margin-top: 1rem;
		color: #64706f;
	}

	.luma-hero__metric {
		padding: 1.2rem;
	}

	.luma-section--muted {
		border-block: 1px solid rgba(42, 54, 65, 0.1);
		background: #f5f0eb;
	}

	.luma-section__intro {
		max-width: 780px;
	}

	.luma-grid {
		display: grid;
		gap: 1rem;
		margin-top: clamp(2rem, 4vw, 3rem);
	}

	.luma-grid--4 {
		grid-template-columns: repeat(4, minmax(0, 1fr));
	}

	.luma-card {
		padding: 1.25rem;
	}

	.luma-card h3 {
		color: #17202a;
		font-size: 1.12rem;
		letter-spacing: -0.02em;
	}

	.luma-card p {
		color: #64706f;
		font-size: 0.9375rem;
		line-height: 1.6;
	}

	.luma-card--compact span {
		display: block;
		width: 2.25rem;
		height: 0.25rem;
		margin-bottom: 1.25rem;
		border-radius: 999px;
		background: #8f6f63;
	}

	.luma-link-cta {
		display: inline-flex;
		margin-top: 2rem;
		color: #17202a;
		font-weight: 800;
	}

	.luma-link-cta:hover {
		color: #6f554b;
	}

	.luma-about__card,
	.luma-final__card {
		padding: clamp(1.5rem, 4vw, 3rem);
		border: 1px solid rgba(42, 54, 65, 0.1);
		border-radius: 24px;
		background: #ffffff;
	}

	.luma-flow {
		display: grid;
		grid-template-columns: repeat(4, minmax(0, 1fr));
		gap: 1rem;
		margin-top: 2rem;
	}

	.luma-flow__step {
		padding: 1.2rem;
	}

	.luma-flow__step span {
		display: inline-flex;
		margin-bottom: 1rem;
		color: #8f6f63;
		font-size: 0.75rem;
		font-weight: 800;
	}

	.luma-flow__step p {
		margin: 0;
		color: #17202a;
		font-weight: 800;
	}

	.luma-final {
		background: #efe7e1;
	}

	.luma-final__card {
		display: grid;
		grid-template-columns: minmax(0, 1fr) auto;
		gap: 2rem;
		align-items: end;
		background: #17202a;
	}

	.luma-final h2,
	.luma-final .luma-lead {
		color: #ffffff;
	}

	.luma-final .luma-lead {
		opacity: 0.78;
	}

	@media (max-width: 980px) {
		.luma-hero__inner,
		.luma-grid--4,
		.luma-flow,
		.luma-final__card {
			grid-template-columns: repeat(2, minmax(0, 1fr));
		}
	}

	@media (max-width: 700px) {
		.luma-hero__inner,
		.luma-grid--4,
		.luma-flow,
		.luma-final__card {
			grid-template-columns: 1fr;
		}

		.luma-hero h1 {
			max-width: 760px;
		}
	}
</style>
