<header class="site-header">
	<div class="site-container site-header__inner">
		<a class="site-header__brand" href="/" aria-label="Future Solutions home">
			<span>Future</span>
			<span>Solutions</span>
		</a>
		<nav class="site-header__nav" aria-label="Main navigation">
			<a href="/templates">Templates</a>
			<a href="/#services">Services</a>
			<a href="/#seo">SEO</a>
			<a href="/#backend">Backend Systems</a>
			<a href="/#process">Resources</a>
		</nav>
		<a class="site-header__cta" href="/#audit">Get a Website Audit</a>
	</div>
</header>

<style>
	.site-header {
		position: sticky;
		top: 0;
		z-index: 10;
		background: color-mix(in srgb, var(--color-bg) 92%, transparent);
		border-bottom: 1px solid var(--color-border);
		backdrop-filter: blur(16px);
	}

	.site-header__inner {
		display: flex;
		align-items: center;
		justify-content: space-between;
		gap: 1.5rem;
		min-height: 5rem;
	}

	.site-header__brand {
		display: inline-flex;
		align-items: baseline;
		gap: 0.25rem;
		color: var(--color-text);
		font-size: 1.05rem;
		font-weight: 700;
		letter-spacing: -0.035em;
		white-space: nowrap;
	}

	.site-header__brand span:last-child {
		color: var(--color-accent-dark);
	}

	.site-header__nav {
		display: flex;
		align-items: center;
		justify-content: flex-end;
		gap: 1.1rem;
		color: var(--color-muted);
		font-size: 0.9rem;
		font-weight: 600;
	}

	.site-header__nav a {
		padding-block: 0.35rem;
		white-space: nowrap;
	}

	.site-header__nav a:hover {
		color: var(--color-text);
	}

	.site-header__cta {
		display: inline-flex;
		align-items: center;
		justify-content: center;
		min-height: 2.4rem;
		padding: 0.65rem 0.85rem;
		border: 1px solid color-mix(in srgb, var(--color-accent) 34%, var(--color-border));
		border-radius: var(--radius-sm);
		background: var(--color-white);
		color: var(--color-text);
		font-size: 0.875rem;
		font-weight: 700;
		line-height: 1;
		white-space: nowrap;
	}

	.site-header__cta:hover {
		border-color: var(--color-accent);
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
	}

	@media (max-width: 1040px) {
		.site-header__inner {
			gap: 1rem;
		}

		.site-header__nav {
			gap: 0.8rem;
			font-size: 0.84rem;
		}
	}

	@media (max-width: 860px) {
		.site-header__nav {
			display: none;
		}

		.site-header__inner {
			min-height: 4.5rem;
		}
	}

	@media (max-width: 440px) {
		.site-header__cta {
			padding-inline: 0.7rem;
			font-size: 0.8125rem;
		}
	}

	@media (max-width: 380px) {
		.site-header__cta {
			display: none;
		}
	}
</style>
