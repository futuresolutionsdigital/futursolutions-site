<div class="system-preview" aria-label="Website system preview">
	<div class="system-preview__browser">
		<div class="system-preview__chrome" aria-hidden="true">
			<span></span>
			<span></span>
			<span></span>
		</div>

		<div class="system-preview__content">
			<div class="system-preview__topline">
				<span>Practice Growth System</span>
				<span class="system-preview__status">Live</span>
			</div>

			<div class="system-preview__hero-card card">
				<p class="system-preview__label">Website</p>
				<h3>Modern practice homepage</h3>
				<div class="system-preview__meter" aria-hidden="true">
					<span></span>
				</div>
			</div>

			<div class="system-preview__flow" aria-label="Website to lead system flow">
				<span>Website</span>
				<span>Lead Capture</span>
				<span>Booking</span>
				<span>Follow-up</span>
				<span>SEO</span>
			</div>

			<div class="system-preview__grid">
				<div class="system-preview__module card">
					<p class="system-preview__label">New inquiry captured</p>
					<p>Name · Email · Service interest</p>
				</div>

				<div class="system-preview__module card">
					<p class="system-preview__label">Qualified Lead</p>
					<p>Consultation requested: Tuesday · 10:30 AM</p>
				</div>

				<div class="system-preview__module card system-preview__module--wide">
					<p class="system-preview__label">Follow-up sequence active</p>
					<p>Email 1 sent · Reminder scheduled</p>
				</div>
			</div>
		</div>
	</div>

	<div class="system-preview__seo card">
		<p>Local SEO Foundation</p>
		<span>Service pages · Metadata · Internal links</span>
	</div>
</div>

<style>
	.system-preview {
		position: relative;
		max-width: 560px;
		margin-inline: auto;
	}

	.system-preview__browser {
		overflow: hidden;
		border: 1px solid var(--color-border);
		border-radius: 18px;
		background: var(--color-white);
		box-shadow: 0 24px 70px rgba(15, 23, 42, 0.08);
	}

	.system-preview__chrome {
		display: flex;
		gap: 0.45rem;
		align-items: center;
		padding: 0.9rem 1rem;
		border-bottom: 1px solid var(--color-border);
		background: var(--color-bg-secondary);
	}

	.system-preview__chrome span {
		width: 0.55rem;
		height: 0.55rem;
		border: 1px solid color-mix(in srgb, var(--color-muted) 26%, transparent);
		border-radius: 999px;
		background: var(--color-white);
	}

	.system-preview__content {
		display: grid;
		gap: 1rem;
		padding: clamp(1rem, 2.5vw, 1.5rem);
		background:
			linear-gradient(90deg, rgba(226, 232, 240, 0.45) 1px, transparent 1px),
			linear-gradient(180deg, rgba(226, 232, 240, 0.45) 1px, transparent 1px),
			var(--color-bg);
		background-size: 44px 44px;
	}

	.system-preview__topline,
	.system-preview__flow {
		display: flex;
		align-items: center;
		color: var(--color-muted);
		font-size: 0.8125rem;
		font-weight: 600;
	}

	.system-preview__topline {
		justify-content: space-between;
	}

	.system-preview__status {
		padding: 0.25rem 0.55rem;
		border-radius: 999px;
		background: var(--color-accent-light);
		color: var(--color-accent-dark);
	}

	.system-preview__hero-card,
	.system-preview__module {
		padding: 1rem;
	}

	.system-preview__hero-card h3 {
		margin-bottom: 1rem;
		font-size: 1.125rem;
		letter-spacing: -0.02em;
	}

	.system-preview__label {
		margin-bottom: 0.4rem;
		color: var(--color-accent-dark);
		font-size: 0.75rem;
		font-weight: 700;
		letter-spacing: 0.08em;
		text-transform: uppercase;
	}

	.system-preview__meter {
		height: 0.55rem;
		overflow: hidden;
		border-radius: 999px;
		background: var(--color-accent-light);
	}

	.system-preview__meter span {
		display: block;
		width: 72%;
		height: 100%;
		border-radius: inherit;
		background: var(--color-accent);
	}

	.system-preview__flow {
		flex-wrap: wrap;
		gap: 0.45rem;
	}

	.system-preview__flow span {
		display: inline-flex;
		align-items: center;
		gap: 0.45rem;
		padding: 0.42rem 0.6rem;
		border: 1px solid var(--color-border);
		border-radius: 999px;
		background: rgba(255, 255, 255, 0.78);
		color: var(--color-text);
	}

	.system-preview__grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: 0.85rem;
	}

	.system-preview__module p:last-child {
		color: var(--color-muted);
		font-size: 0.9rem;
		line-height: 1.45;
	}

	.system-preview__module--wide {
		grid-column: 1 / -1;
	}

	.system-preview__seo {
		position: absolute;
		right: -1.25rem;
		bottom: -1.5rem;
		max-width: 250px;
		padding: 0.9rem 1rem;
	}

	.system-preview__seo p {
		margin-bottom: 0.25rem;
		color: var(--color-text);
		font-size: 0.9rem;
		font-weight: 700;
	}

	.system-preview__seo span {
		color: var(--color-muted);
		font-size: 0.8125rem;
		line-height: 1.4;
	}

	@media (max-width: 980px) {
		.system-preview {
			margin-top: 1rem;
		}
	}

	@media (max-width: 620px) {
		.system-preview__grid {
			grid-template-columns: 1fr;
		}

		.system-preview__seo {
			position: static;
			max-width: none;
			margin-top: 0.85rem;
		}
	}
</style>
